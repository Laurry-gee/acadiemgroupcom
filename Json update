Instead of generating an image, outputs information about the image in JPEG format. The JPEG object will contain data such as image size before and after resizing, source image's MIME type, and file size.
